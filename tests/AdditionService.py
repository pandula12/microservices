def add(data):

    total = 0
    # loop through each number and add it to the total
    for num in data:
        total += num

    # prepare response object
    response = total

    # return resulting number
    return response